# Core Python dependencies
python-dotenv==1.1.1

# AWS and S3 dependencies
boto3==1.35.28
botocore==1.35.99

# OpenAI API for SEA-LION translation
openai==1.106.1

# Audio processing and transcription dependencies
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.19.0
accelerate>=0.20.0

# WhisperX specific dependencies
faster-whisper>=0.9.0
pyannote-audio>=3.1.0

# Additional audio processing
librosa>=0.10.0
soundfile>=0.12.1

# Clinical extraction dependencies
sentencepiece>=0.1.99
protobuf>=3.20.0

# Utility dependencies
tqdm>=4.67.1
requests>=2.32.5

# Standard library dependencies that may be needed
typing-extensions>=4.15.0
pathlib2; python_version < "3.4"

# Optional: HuggingFace Hub for model downloads (already included in transformers)
# huggingface-hub>=0.16.0  # Not needed - included with transformers

# Note: WhisperX installed separately via .ebextensions/01_install_whisperx.config 
git+https://github.com/m-bain/whisperx.git