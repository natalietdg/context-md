# Frontend environment variables
# Copy to .env and fill values

# Base URL of your backend API (NestJS)
# For local development:
REACT_APP_API_BASE_URL=http://localhost:4000

# Optional: API key if your backend requires x-api-key
# Leave empty if API_KEY_REQUIRED=false on the backend
REACT_APP_API_KEY=
