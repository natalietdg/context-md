packages:
  yum:
    python3: []       # Ensure Python 3 is available
    git: []           # if your requirements need it
    ffmpeg: []        # if WhisperX needs ffmpeg

container_commands:
  01_create_venv:
    command: "python3 -m venv /var/app/staging/.venv"
  02_pip_upgrade:
    command: "/var/app/staging/.venv/bin/pip install --upgrade pip"
  03_pip_install:
    command: "/var/app/staging/.venv/bin/pip install -r /var/app/staging/requirements.txt"
